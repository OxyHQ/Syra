X-Syra-Json
{"subject":"Keep playlist activity inside storage and ordering regression gates","files":{"packages/backend/src/services/playlists/collaboration.ts":{"replacements":[{"old":"and, count, desc, eq, gt, lte","new":"and, count, eq, gt, lte"},{"old":"import { getDb } from '../../db/postgres';","new":"import { getDb } from '../../db/postgres';\nimport { descNullsLast } from '../../db/catalog/containers';"},{"old":"desc(playlistActivity.createdAt), desc(playlistActivity.id)","new":"descNullsLast(playlistActivity.createdAt), descNullsLast(playlistActivity.id)"}]},"packages/shared-types/src/playlist-sharing.ts":{"replacements":[{"old":"export const playlistActivityResponseSchema = z.object({ items: z.array(z.object({","new":"export const playlistActivitySchema = z.object({"},{"old":"})) });","new":"});\nexport const playlistActivityResponseSchema = z.object({ items: z.array(playlistActivitySchema) });"}]},"packages/backend/src/db/__tests__/zodPathsExistInDrizzle.test.ts":{"replacements":[{"old":"  playlists: 'playlistSchema',","new":"  playlists: 'playlistSchema',\n  playlistActivity: 'playlistActivitySchema',"},{"old":"  listeningEvents: 'server-internal — play telemetry',","new":"  listeningEvents: 'server-internal — play telemetry',\n  playlistInvites: 'server-internal — stored bearer-grant digests; the creation response is a newly issued token, not a row DTO',"}]}}}
