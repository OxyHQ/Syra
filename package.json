{
  "name": "syra",
  "version": "2.0.0",
  "description": "Syra Monorepo - Music Streaming App Frontend and Backend",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "dev:frontend": "npm run dev --workspace=@syra/frontend",
    "dev:backend": "npm run dev --workspace=@syra/backend",
    "build": "npm run build --workspaces",
    "build:shared-types": "npm run build --workspace=@syra/shared-types",
    "build:frontend": "npm run build --workspace=@syra/frontend",
    "build:backend": "npm run build --workspace=@syra/backend",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "clean": "npm run clean --workspaces && rm -rf node_modules",
    "install:all": "npm install && npm install --workspaces",
    "start:frontend": "npm run start --workspace=@syra/frontend",
    "start:backend": "npm run start --workspace=@syra/backend",
    "postinstall": "npm run build --workspace=@syra/shared-types"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "react-native-worklets": "0.5.1",
    "typescript": "~5.9.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "@oxyhq/services": "6.9.12",
    "@oxyhq/core": "1.11.11",
    "@react-native/js-polyfills": "^0.81.1",
    "react-native-css-interop": "^0.2.0",
    "react-native-worklets": "0.5.1",
    "react-native-worklets-core": "^1.6.2"
  }
}
